# Server
PORT=8080
CORS_ORIGIN=http://localhost:5173

# Database (docker-compose default)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/hedera_sessions

# Hedera
HEDERA_NETWORK=testnet
HEDERA_SPENDER_ACCOUNT_ID=0.0.5500440
HEDERA_SPENDER_PRIVATE_KEY=0x99587c669d4120a3bb4cfe7676759b776bbc4ec4644bf501b4a43953a3d88058

# Mirror node
MIRROR_NODE_BASE_URL=https://testnet.mirrornode.hedera.com/api/v1

# Defaults
DEFAULT_CAP_HBAR=1
DEFAULT_EXPIRY_HOURS=24

# Idempotency window (seconds)
IDEMPOTENCY_BUCKET_SECONDS=300

# Safety for fees (tinybars)
MIN_SPENDER_BALANCE_TINYBARS=1000000
