services:
  db:
    image: postgres:16
    container_name: hedera_sessions_db
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: hedera_sessions
    ports:
      - "5432:5432"
    volumes:
      - ./pgdata:/var/lib/postgresql/data
